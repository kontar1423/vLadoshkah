# 🐾 Backend Service

Этот каталог содержит весь исполняемый код API. Ниже — краткое руководство по запуску и тестированию бэкенда.

## ⚙️ Требования

- Node.js ≥ 20 и npm ≥ 10
- Docker + Docker Compose (для запуска всего стека локально)
- PostgreSQL, Redis, MinIO и Kafka (используемся через `docker-compose.yml`)

## 🚀 Быстрый старт

1. Установите зависимости:
   ```bash
   npm install
   ```
2. Настройте `.env` (см. пример в корне проекта).
3. Примените миграции:
   ```bash
   npm run migrate
   ```
4. Запустите приложение:
   ```bash
   npm run dev    # с hot-reload через nodemon
   npm start      # production-режим
   ```

Для полного стека используйте:

```bash
docker compose up --build
```

## 🧪 Тесты

```bash
npm test
```

Jest покрывает DAO/роуты/сервисы/миддлвары (20 suites). `NODE_OPTIONS=--experimental-vm-modules` выставляется автоматически.

## 📂 Структура

- `index.js` — точка входа сервера
- `controllers/`, `routes/`, `services/`, `dao/` — основной слой API
- `middlewares/`, `validators/` — проверки и пайплайны
- `config/` — конфигурация (JWT, bcrypt и т.д.)
- `messaging/`, `cache/`, `utils/` — интеграции Kafka/Redis и утилиты
- `logger.js`, `db.js`, `initMinio.js`, `minioClient.js` — инфраструктурные модули

## 🛠️ Полезные команды

- `npm run migrate` / `npm run migrate:down` — управление миграциями
- `npm test` — весь набор тестов
- `npm run dev` — локальная разработка
- `npm start` — запуск в production-режиме

> За документацией по REST API обращайтесь к `README.md` в корне репозитория (для фронтенд-команды).

